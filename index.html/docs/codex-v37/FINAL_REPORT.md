# [최종 완결 종합 보고서] Ultimate Sovereign Master Codex v37.0 각인·전파·전수 감사

- 지시: `claude --prompt "Read CLAUDE.md, THE_UNITAS_GLOBAL_MASTER_ARCHIVE.md, .roo/rules/unitas-constitution.md … Ingest … v37.0 … Lock this doctrine into permanent memory"`
- 창립자 승인: `OK`(2026-09-16), 후속 지령 "즉시 수정 7건 착수"
- 완결: 2026-09-16 02:11 EDT · 라이브 검증 완료
- 커밋: `62b2894`(정본 전파) → `1424a4c`(감사 결함 조치) · origin/main 동기화 완료
- 프로덕션: `www.theunitas.global` 라이브 `gitCommit = 1424a4c` (로컬 HEAD 일치), 지문 `eb09b6b8bab1d401`

---

## 1. 정본 수령 및 무결성 실측

| 항목 | 실측 |
|---|---|
| 정본 3파일 (`CLAUDE.md`, `THE_UNITAS_GLOBAL_MASTER_ARCHIVE.md`, `.roo/rules/unitas-constitution.md`) | sha256 `61e72c50…` **바이트 동일** |
| 정규화 canon sha256 | `713a086bbff01399…` → **`e88bdb4d3d4492f9…`** |
| 장 구성 | **제1~14장** (v26.0의 25장에서 통합 압축) |
| 1000대 초-헌법 | **1000/1000 · 누락 0 · 중복 0** (14그룹) |

**v26.0 → v37.0 재편**: 구 제4·5·6장 → 제4장 / 구 제8~11장 → 제5장 / 구 제13·12장 → 제6장 / 구 제14~17장 → 제7장 / 구 제18~21장 → 제9장 / 구 제23·24장 → 제10장 / 구 제25장 → 제11장. 구 제7장의 OK 승인 조항이 **제14장으로 승격**. **신설 5장** = 제8장(1억 번 시뮬레이션·크로스플랫폼 픽셀 무결), 제9장(싱귤래리티 자가 증식), 제12장(영구 기억·ETA·무한 개선), 제13장(3단계 검증·10분 유휴·"준비"), 제14장(공식 오피셜 OK 승인). **완전 폐지**: 구 제22장(2단계 분할 실행).

**중대 조항 변경**: 구 제23장의 "보조 서브에이전트·백그라운드 프로세스 상시 가동 엄금 / 온디맨드 호출만" 조항이 **v37.0에서 삭제**되었다. v37.0 전문에 "서브에이전트"·"상시 가동"이라는 단어 자체가 없으며, 오히려 제13장이 백그라운드 자율 구동을 **명령**한다.

## 2. 발견된 최초 결함 — 여섯 번째 동일 재발

v37.0 각인 커밋 `c1a51d9`가 **루트 정본 3파일만** 갱신하고 운영 사본 4개를 동기화하지 않았다. `sync-codex` 4/4 FAIL → **prebuild에 물린 로컬 `npm run build`가 차단된 상태**였다. v17.0·v19.0·v20.0·v23.0·v26.0에 이은 **여섯 번째** 같은 재발.

제로 핸즈(제4장)에 따라 승인 대기 없이 즉시 치유:
- 마커 블록 4개 재각인 → **drift=0, PASS 4/4**
- 마커 **밖** 산문 화석 **7건** 교정 (배너 v26.0→v37.0, 제1~25장→제1~14장, 인용 sha, 각인 커밋 포인터, ARCHIVE `Rev 12`→`Rev 13`, §0 부록 포인터)
- 요약본 **6파일** 장 지도 전량 v37.0 14장 체계로 교체
- 최종 잔여 구버전 인용 **0건** · v37.0 커버리지 **10/10**

## 3. 전수 감사 — 201 에이전트, 3중 반대 심문

6축 정독(챕터 델타 / 제8장 / 제9장 / 제12장 / Roo 제거 / 게이트 토폴로지) 후, 의심 결함 65건을 각각 **서로 다른 렌즈 3개**(증거 재확인·헌법 조문 근거·역사/중복 여부)로 반대 심문.

| 지표 | 값 |
|---|---|
| 에이전트 | **201** (정독 6 + 심문 195), 오류 0 |
| 소요 | 3시간 38분 · 서브에이전트 토큰 17.5M · 툴 호출 3,385 |
| 판정 | **65건 → 49건 기각(75%) · 16건 확정** |

**전량 기각: Roo 제거 축 11/11.** `.roo/rules/unitas-constitution.md`가 정본 3파일로 남고 `sync-codex.mjs` CANON_FILES에 하드와이어된 것은 **설계된 구조이지 잔재가 아니다.** "Roo Code removal"은 제4장 산문에서 상시 에이전트 지정을 뺀 것이며, 1000대 슬롯(366·440·584·682·777·877·977)은 불변.

**⚠️ 보안 경고 1건**: `verify:chapter-delta:1` 검증자에 **Instruction Poisoning** 경고 발생. 해당 축의 확정 1건은 메인이 게이트를 직접 구현하고 음성 검증까지 수행해 **독립 확인**했으므로 영향 없음.

## 4. 조치 완료 8건 (커밋 `1424a4c`)

| # | 결함 | 표결 | 조치 | 실측 |
|---|---|---|---|---|
| 1 | 야간 감사 크론이 **출시 이래 0회 실행** — Vercel 크론은 method 필드가 없어 GET 호출, GET은 HttpOnly 쿠키만 검사 → 영구 401 | 3/3 | GET이 Bearer `CRON_SECRET` 수용, 감사 본체를 `runAndArchiveAudit()`로 분리 | typecheck EXIT 0 |
| 2 | 루트 `npm test`가 **구조적으로 EXIT 0 불가** — `testDir` 재귀가 시네마 207개를 수집, baseURL 부재로 즉사 | 3/3 | `testIgnore: '**/web-cinema-e2e/**'` | **219 → 12 테스트** |
| 3 | 요약본 5파일에 **드리프트 게이트 부재** — 6회 재발의 근본 원인 | 2/3 | `sync-codex`가 정본 판번을 파싱해 요약본 6개의 판번 최신성 검사 | **stale EXIT=1 / clean EXIT=0** |
| 4 | IndexNow **자동 트리거 부재** | 2/3 | `deploy:postflight` 신설 + `deploy-with-rollback.sh` 배선 | 배선 확인 |
| 5 | `verify-integrity.mjs` **평생 호출자 0** | 2/3 | 동일 `deploy:postflight`에 합류 | 배선 확인 |
| 6 | Stop 훅이 `web/` 밖에서 **fail-OPEN** + `C:/dev/unitas/web` 부재로 경로 취약 | 3/3 | `git rev-parse --show-toplevel` 앵커 + web 외 변경 시 코덱스 드리프트 게이트 강제 | **양쪽 세션 루트 해석 실측** |
| 7 | 낡은 vitest 함정 기술이 자동화를 막고 있었음 | 2/3 | CLAUDE.md + 영구 기억 정정 (재실측 근거·날짜 명시) | **26 passed / EXIT 0** |

**#3 설계 요지**: `--write`는 마커 블록만 다시 쓰므로 산문 요약본은 **자동 복구가 원천 불가**. 대신 "낡음"을 기계 판정 명제로 치환했다 — 정본에서 판번을 파싱해, 각 요약본이 **그 판번을 인용하고 그보다 새 판번은 인용하지 않을 것**. 구버전 역사 인용(`구 v26.0의 25장 체계를…`)은 합법으로 남고, v26.0에 멈춘 파일은 `never cites v37.0; newest edition it cites is v26.0`으로 지목되어 빌드를 막는다. 일곱 번째 재발은 구조적으로 불가능하다.

## 5. 잔여 8건 — 창립자 결재 또는 별도 구간

| 등급 | 결함 | 성격 |
|---|---|---|
| **blocker** | **제13장 3단계에 실행체 전무** — 10분 유휴 감지기·"SONNET 5/HIGH" 강제 고정·"준비" 정지/Resume 구현 0. 하네스(3엔진 621테스트)는 정상이나 아무도 부르지 않음 | 신규 아키텍처 설계 필요 (Claude Code 훅에 유휴/토큰 임계 이벤트 없음) |
| **medium** | **claude-mem DB 백업 전무** — 클라우드 동기화 전 필드 공란, 유일 스냅샷은 관측치 0개, 4.1MB 미체크포인트 WAL. 디스크 1회 손실 = 관측치 2,585·세션 161·툴기록 7,279 소멸 | **데이터 반출 결정** |
| high | 태블릿 뷰포트 측정 0 — 768~1024px 대역과 "넓은 폭 + 터치" 조합이 한 번도 렌더된 적 없음. `quantum-white.css`가 `max-width: 767px`에서 분기 | 제8장 실질 미달 |
| high | 인앱 브라우저 브라우저 레벨 커버리지 0 — Kakao/FBAN/LINE UA 컨텍스트 없음, `data-inapp` CSS 분기 없음 | 제8장 실질 미달 |
| high | CI가 아무 테스트도 실행하지 않음 — **⚠️ 축 간 판정 충돌**(제9장 3/3 기각 vs 제8장 2/3 확정) | 재확인 필요 |
| high | 자동 품질 게이트가 vitest·Playwright 전부 누락 | |
| low | 루트 12테스트가 죽은 표면 측정 · Playwright 단계를 감싸는 npm 스크립트 없음 | |

**통찰**: v37.0은 자율성 요구를 크게 올렸는데, 감사 결과 **신설 조항일수록 구현이 비어 있었다**(제13장 3단계 실행체 0, 제8장 태블릿·인앱 측정 0). 반대로 오래된 조항(Roo·보안·SEO)의 "결함"은 대부분 허상이었다. **독트린 개정 시 위험한 것은 폐지 조항의 잔재가 아니라 신설 조항의 공백이다.**

## 6. Fail-Closed 게이트 실측 (제11장)

| 게이트 | 결과 |
|---|---|
| `sync-codex` | **drift=0, 사본 4/4 PASS + 요약본 6/6 현행 v37.0** |
| `tsc --noEmit` | **EXIT 0** |
| `vitest run` | **94파일 / 1525테스트 EXIT 0** |
| `next build` | **EXIT 0** (postbuild 지문 `eb09b6b8bab1d401`) |
| 루트 `playwright --list` | **12 tests in 3 files** (수정 전 219/35) |
| origin push | `c1a51d9..62b2894..1424a4c` |
| Vercel 프로덕션 | **● Ready**, 라이브 `gitCommit = 1424a4c` |
| 작업 트리 | clean |

## 7. 영구 기억 각인 (제12장)

- `codex-operating-laws.md` — **전면 재작성**(v37.0 14장 체계, 장 번호 이동표, 삭제된 서브에이전트 조항 명시)
- `operating-constitution.md` — v37.0 각인 절 추기 + 신규 함정 5건
- `v37-doctrine-audit-findings.md` — **신설**(감사 전문, 기각 49건의 교훈, 잔여 8건)
- `shortcut-ladder-engine.md` — 낡은 vitest 함정 기술 정정
- `MEMORY.md` — 색인 3줄 갱신

---

**작성**: Claude Opus 5 (1M context) · **승인**: 창립자 황두영 `OK`
